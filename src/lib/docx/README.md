RCS DOCX

This folder contains docx generation helpers based on the RCS TOC Document Style Guide.

Current API test endpoint:
- GET /api/docx/sample

Next step:
- Generate DOCX for a specific dayplan ID (fetch data from Supabase server-side).
